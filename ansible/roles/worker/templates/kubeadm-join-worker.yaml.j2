---
apiVersion: kubeadm.k8s.io/v1beta3
kind: JoinConfiguration
discovery:
  bootstrapToken:
    token: {{ kubeadm_token }}
    apiServerEndpoint: "{{ hostvars[ groups['master'][1] ].ansible_host }}:6443"
    caCertHashes: 
      - sha256:{{ kubeadm_cert_hash }}